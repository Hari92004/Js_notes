// here we are gonna to discuss about the form and its validation ---what it is and why it is necessary to  know---
let naam=document.querySelector("#name")
let form=document.querySelector("form")
form.addEventListener("submit",function(dets){
//  dets.preventDefault();
 if(naam.valueOf.length>2){
  
 }
})

// whta is regex---->
// this is the type of text for validation email's pattern,name,password etc for form validation


// syntax for applying regex in the form validation ---
// let emailans=emailRegex.test(email.value)
//let passwordans=passwordRegex.test(password.value)

//if(!emailans){
//  document.querySelector("#emailError")
// }/